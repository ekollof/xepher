# Maintainer: Emiel Kollof <emiel@kollof.nl>
pkgname=weechat-xmpp
pkgver=0.2.0
pkgrel=1
pkgdesc="WeeChat plugin for XMPP/Jabber protocol"
arch=('x86_64' 'aarch64')
url="https://git.hackerheaven.org/ekollof/weechat-xmpp-fixed"
license=('MPL2')
depends=(
    'weechat'
    'libstrophe'
    'libxml2'
    'lmdb'
    'libsignal-protocol-c'
    'gpgme'
    'fmt'
    'curl'
    'openssl'
)
makedepends=(
    'gcc>=12'
    'git'
    'bison'
    'flex'
)
source=("git+https://git.hackerheaven.org/ekollof/weechat-xmpp-fixed.git#tag=v${pkgver}")
sha256sums=('SKIP')

prepare() {
    cd "$srcdir/$pkgname"
    git submodule update --init --recursive
}

build() {
    cd "$srcdir/$pkgname"
    make weechat-xmpp
}

package() {
    cd "$srcdir/$pkgname"
    install -Dm755 xmpp.so "$pkgdir/usr/lib/weechat/plugins/xmpp.so"
    install -Dm644 LICENSE "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
    install -Dm644 README.org "$pkgdir/usr/share/doc/$pkgname/README.org"
}
